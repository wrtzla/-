import React from 'react';

export const Background: React.FC = () => {
  return (
    <div className="fixed inset-0 w-full h-full -z-10 bg-[#008080]">
      {/* Optional: Add a subtle pattern overlay if desired, but solid teal is classic */}
    </div>
  );
};